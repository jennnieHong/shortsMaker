# ShortsMaker Project Roadmap

## 🎯 프로젝트 개요
*   **목표:** CapCut과 유사한 성능과 사용성을 가진 템플릿 기반의 **Windows 데스크톱 영상 에디터** 구축
*   **특징:** 무거운 웹뷰 렌더링을 피하고, 바닥단(Core)에 C/C++ 엔진을 직접 붙여 압도적인 렌더링 속도와 안정성을 확보하는 아키텍처

---

## 🏗️ 아키텍처 결정 사항 및 대안 비교

### 1. 플랫폼 및 데스크톱 래핑
*   **대안 A (웹 브라우저 기반):** 접근성은 좋으나 영상 인코딩 시 메모리 누수 및 속도 한계 명확 (초기 보류)
*   **대안 B (Electron + React) - ✅ 선택됨:** 기존 웹 프론트엔드 개발 지식을 100% 활용하면서도, Node.js를 통해 PC의 네이티브 하드웨어 자원에 접근할 수 있어 생산성과 성능을 모두 잡을 수 있음.

### 2. 코어 비디오 엔진 아키텍처 (핵심)
*   **대안 A (프로세스 호출 방식):** `ffmpeg.exe`를 몰래 실행시켜 명령어를 전달하는 방식. 개발이 쉽지만 성능의 한계와 제어의 정밀함이 떨어짐.
*   **대안 B (C++ Native Addon 방식) - ✅ 선택됨:** 실제 CapCut이 사용하는 구조. Node.js의 `N-API`를 사용해 프론트엔드(React)에서 직접 C++ 메모리 영역의 코드를 함수처럼 호출. 초기 세팅 난이도는 높으나 **"가장 빠르고 가벼운"** 퍼포먼스를 낼 수 있음.

---

## 📝 작업 순서 및 현재 진행 상황

### 1단계: 프로젝트 기본 뼈대 구성 (완료)
*   [x] `frontend/`: React + Vite 기반의 에디터 UI 골격 생성 (다크모드, 캔버스 영역, 타임라인 영역 배치)
*   [x] `engine/`: C++ 코어 엔진 폴더 생성 및 소스코드(`main.cpp`), 설정 파일(`binding.gyp`) 작성

### 2단계: C++ 빌드 환경 구축 (완료)
*   [x] 윈도우 환경에 Visual Studio C++ 빌드 도구 설치 및 재부팅
*   [x] `engine` 폴더 안에서 `npm run build` (`node-gyp rebuild`)를 실행하여 C++ 코드를 자바스크립트 모듈(`.node`)로 컴파일

### 3단계: Electron 통합 및 C++ 연결 (완료)
*   [x] 루트 폴더에 Electron 환경 세팅
*   [x] Electron을 띄울 때 `frontend`의 화면을 불러오고, 백그라운드에서는 `engine`의 `.node` 모듈을 로드하도록 연결
*   [x] 프론트엔드(React)에서 버튼을 눌렀을 때 C++ 코드가 정상적으로 응답하는지 통신 테스트

### 4단계: FFmpeg C 라이브러리 (FFmpeg API) 연동
*   [ ] C++ 엔진 내부에서 껍데기 코드를 지우고, 실제 영상 처리(디코딩/인코딩/크롭 등)를 수행하는 C언어 로직 작성
*   [ ] GPU 하드웨어 가속기(NVENC 등) 활성화 옵션 추가

### 5단계: 프론트엔드 고도화
*   [ ] `Konva.js`를 활용한 텍스트/이펙트 배치 WYSIWYG 구현
*   [ ] 템플릿(JSON) 파싱 및 타임라인 동기화
