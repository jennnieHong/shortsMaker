# ShortsMaker 🎬 프로젝트 개발 로드맵 (상세 마스터 플랜)

이 문서는 빈 껍데기 폴더에서 시작해 전문가용 숏폼 영상 편집기로 진화해 온 **ShortsMaker 프로젝트의 전체 개발 역사와 향후 계획**을 상세히 기록한 마스터 로드맵입니다.

## 📌 프로젝트 개요 및 기술 스택
*   **목표**: 데스크톱(Windows) 환경에서 원본 가로 영상들을 빠르고 직관적으로 컷 편집하여 세로 숏폼(9:16) 규격으로 렌더링해주는 독립형 영상 편집기(CapCut 스타일) 개발.
*   **프론트엔드 (UI)**: `React.js` + `Vite` (직관적인 타임라인 UI 및 실시간 비디오 캔버스)
*   **애플리케이션 셸**: `Electron` (로컬 파일 시스템 접근 및 C++ 모듈 로딩)
*   **백엔드 (렌더링 엔진)**: `C++ N-API` + `FFmpeg (libavcodec, libswscale)` (초고속 프레임 디코딩 및 인코딩)

## 🤔 주요 기술 결정 사항 및 대안 비교 (Architecture Decisions)
프로젝트 초기에 비디오 렌더링을 처리할 엔진 아키텍처를 두고 다음과 같은 대안들을 비교 및 검토했습니다.

1.  **대안 A: 웹 브라우저 기반 (Remotion / WebCodecs)**
    *   *장점*: JS/React만으로 구현 가능, 트랜지션 및 텍스트 효과 구현이 쉬움.
    *   *단점*: 대용량 원본 영상(4K 등) 디코딩 시 브라우저 메모리 한계로 크래시 발생 잦음. 렌더링 속도가 현저히 느림. **(기각)**
2.  **대안 B: Node.js에서 FFmpeg CLI 호출 (`fluent-ffmpeg` 등)**
    *   *장점*: 구현이 상대적으로 매우 간단함.
    *   *단점*: 세밀한 프레임 단위 제어(커스텀 크롭, 복잡한 자막 버닝)가 어렵고, 렌더링 시마다 새로운 프로세스를 띄워야 하므로 오버헤드가 큼. **(기각)**
3.  **대안 C: C++ Native Addon 기반 엔진 구축 (현재 채택됨 🏆)**
    *   *장점*: **가장 빠르고 강력함.** FFmpeg의 C API를 직접 호출하여 디코딩/인코딩의 모든 파이프라인(PTS, FIFO 등)을 메모리 레벨에서 완벽하게 제어. CapCut 등 실제 상용 영상 편집기들이 사용하는 구조.
    *   *단점*: C++ N-API 연동 초기 설정 및 오디오 믹싱 등 빌드 난이도가 매우 높음.
    *   *결정*: 성능과 확장성을 위해 C++ 엔진을 프론트엔드와 연결하는 방식으로 최종 확정.

---

## 🏗️ 1단계: 프로젝트 뼈대(Skeleton) 구축 및 기반 환경 세팅 (완료)
*   [x] **프로젝트 초기화**: React(Vite) 기반의 `frontend` 폴더와 C++ 기반의 `engine` 폴더 분리
*   [x] **Electron 데스크톱 앱 세팅**: 웹 브라우저가 아닌 독립된 Windows 프로그램으로 실행되도록 `main.js`와 `preload.js`를 구성하여 로컬 파일 시스템 접근 권한 확보
*   [x] **C++ N-API (Node-Addon-API) 환경 구성**: Node.js에서 C++ 코드를 직접 호출할 수 있도록 `binding.gyp`를 작성하고 `node-gyp` 빌드 환경 구축

## 🔌 2단계: 프론트엔드와 C++ 엔진 통신 연결 (완료)
*   [x] **IPC (Inter-Process Communication) 브릿지 구축**: React UI ↔ Electron Main 프로세스 ↔ C++ 엔진 간의 양방향 통신 채널(`invoke`, `handle`) 개설
*   [x] **비동기 렌더링 아키텍처**: 렌더링 시 UI가 멈추지(Freezing) 않도록 C++ 내부에서 `Napi::AsyncWorker`를 상속받아 별도의 백그라운드 스레드에서 무거운 작업을 처리하도록 설계

## 🎞️ 3단계: FFmpeg C++ 코어 엔진(비디오 처리) 기초 공사 (완료)
*   [x] **FFmpeg 라이브러리 링크**: `libavcodec`, `libavformat`, `libswscale` 등 네이티브 라이브러리를 프로젝트에 포함하고 링킹 성공
*   [x] **단일 영상 9:16 변환기**: 가로 영상을 불러와서 정중앙을 세로 숏폼 규격(720x1280)으로 강제 크롭(Crop)하고 H.264 코덱으로 인코딩(`.mp4`)하는 기초 파이프라인 검증

## 🎛️ 4단계: 에디터 UI/UX 및 타임라인 데이터 구조 설계 (완료)
*   [x] **에셋 라이브러리 UI**: 좌측 패널에 내 컴퓨터의 로컬 동영상 파일들을 불러와 목록으로 띄우는 기능 구현
*   [x] **타임라인 상태(JSON) 설계**: 여러 개의 영상을 타임라인에 올릴 때, `path`, `trimStart`, `trimEnd`, `cropX`, `cropY` 등의 메타데이터를 관리하는 `TimelineClip` 구조체 정의
*   [x] **타임라인 트랙 UI**: 영상을 타임라인에 블록 형태로 올리고 삭제하는 인터페이스 구현

## ✂️ 5단계: 정밀 컷 편집 및 플레이어 동기화 (완료)
*   [x] **비디오 클립 구간 자르기(Trim)**: 우측 속성 패널 및 중앙 타임라인 하단에 듀얼 슬라이더(Visual Trimmer)를 배치해 원하는 시간(In/Out)을 시각적으로 잘라내는 기능 완료
*   [x] **플레이헤드(Playhead) 동기화 (WYSIWYG)**: 캔버스의 영상이 타임라인의 현재 재생 시간에 맞춰 정확하게 동기화되어 보이도록 구현 완료
*   [x] **연속 재생 모드 토글**: 다중 클립 편집 시, 클립 간에 끊김 없이 재생할지 단일 클립만 반복할지 결정하는 토글 로직 적용

## 🎥 6단계: C++ 멀티 렌더링 및 오디오 믹싱 고도화 (완료)
*   [x] **멀티 클립 이어붙이기 (Concatenation)**: 타임라인에 놓인 순서대로 C++ 엔진이 영상을 하나씩 열어서 디코딩하고 순서대로 프레임을 이어붙여 인코딩하는 로직 완료
*   [x] **원하는 구간만 고속 인코딩 (Seek & Trim)**: 프론트엔드가 보낸 In/Out 시간에 맞춰 C++이 `av_seek_frame`으로 고속 이동하여 불필요한 디코딩 시간을 획기적으로 단축
*   [x] **오디오(원본 소리) 스트림 무손실 복사**: 영상 처리에 맞춰, 각 클립의 원본 오디오 스트림(AAC 등)을 추출해 타임스탬프(PTS)를 재계산하여 끊김 없이 연속으로 이어붙이는 고성능 Pass-through 믹싱 달성

## 🎯 7단계: 화면 트랜스폼(Transform) 및 최종 효과 연동 (현재 진행 중)
*   [x] **X축 포커스 이동 (좌우 크롭)**: 주인공이 가장자리에 있을 때 프론트엔드 슬라이더로 위치를 맞추고, C++ `sws_scale`이 해당 좌표를 읽어 렌더링하도록 매핑 완료
*   [x] **Y축 이동 및 축소/확대 (Scale/Zoom)**: 영상을 클로즈업(Zoom-in)하여 위아래 숨겨진 여백을 만들고, Y축으로도 자유롭게 패닝(Panning)할 수 있도록 프론트/백엔드 로직 확장 완료
*   [x] **하이브리드 텍스트 오버레이 (자막/워터마크)**: `Konva.js`에서 작성된 화려한 텍스트 디자인을 투명 PNG로 실시간 캡처 후, C++ `libavfilter`를 이용해 영상 프레임 위에 합성(Alpha Blending)하는 하이브리드 엔진 구축 완료

## 🚀 8단계: 에디터 편의성 강화 및 아키텍처 고도화 (미래 과제)
*   [ ] **미디어 오프라인(Media Offline) 감지 및 재연결 기능**: 로컬 원본 파일이 삭제되거나 이동되었을 때, 에셋 목록과 타임라인에 ⚠️경고 아이콘을 띄우고 유실된 파일의 새 경로를 찾아 다시 연결(Relink)하는 프로페셔널 자산 관리 기능
*   [ ] **자유형(펜 도구) 마스크 구현**: `Konva.js` 캔버스 레이어를 활용하여 영상 위에 사용자가 직접 점(Point)을 찍어 다각형(Polygon)을 그리고, 이를 CSS `clip-path` 및 C++ 필터로 변환해 원하는 모양으로 영상을 오려내는 커스텀 마스킹 모드 지원
*   [ ] **원본 비율 미니맵(Minimap) 네비게이터**: 16:9 원본 가로 영상 전체를 썸네일로 띄워두고, 현재 세로(9:16) 캔버스가 전체 원본 중 어느 위치에 해당하는지(크롭 및 줌 상태) 시각적으로 박스 형태로 보여주는 UI 추가
*   [x] **모바일 영상 Auto-Rotate (EXIF 메타데이터 파싱 및 Transpose) 적용**: 스마트폰으로 촬영된 영상의 회전 메타데이터를 파싱하여 C++ 엔진 디코딩 파이프라인에서 자동으로 정방향 회전(Transpose) 시켜주는 기능
*   [x] **다중 텍스트 구간 편집 및 렌더링 아키텍처 고도화**:
    *   **초기 (ASS 기반)**: 프론트엔드에서 자막 데이터를 표준 `.ass` 파일로 컴파일하여 FFmpeg `subtitles` 필터로 초고속 렌더링 적용
    *   **확장 (고급 렌더링)**: 화려한 모션 그래픽과 이펙트를 위해 `Remotion`, `WebGPU`, 또는 `Custom Compositor` 엔진 연동으로 렌더링 파이프라인 확장
*   [ ] **자체 GPU 그래픽 엔진 밑바닥 구축**: OpenGL/Vulkan을 연동하여 오프스크린 렌더링 컨텍스트 구축 및 GLSL 쉐이더 탑재 (초장기 과제)
*   [ ] GPU 하드웨어 가속기(NVENC/VideoToolbox) 활성화
*   [ ] Electron Builder를 통한 `.exe` 실행 파일 패키징 및 배포
